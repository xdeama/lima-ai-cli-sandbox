# VM User and Credentials
VM_NAME=claude
VM_USER=username
VM_INIT_PASS=init

# Port config
VM_SSH_PORT=8222

# Host Absolute Paths
## Path to the folder that contains your ssh public key for connecting to the vm (named key.pub)
PATH_LIMA_SSH=/absolute/path
## Path to the workspace folder that Claude Code is allowed to write to (but not change permissions)
PATH_REPOSITORY=/absolute/path
## SonarQube MCP server credentials
SONARQUBE_TOKEN=your-token
SONARQUBE_URL=https://sonar.yourcompany.com

## Path to the .claude config folder (by default $HOME/.claude for local installations)
## Mounted into the VM for persistence across rebuilds
## Also store .claude.json inside this folder — it will be symlinked to ~/.claude.json in the VM
PATH_LIMA_CLAUDE=/absolute/path/.claude

## Path to the .gemini config folder (by default $HOME/.gemini for local installations)
## Mounted into the VM for persistence across rebuilds
PATH_LIMA_GEMINI=/absolute/path/.gemini
